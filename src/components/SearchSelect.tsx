import React,{useEffect,useId,useMemo,useState}from'react'
export type SearchOption={value:string;label:string}
export default function SearchSelect({value,onChange,options,placeholder='Digite para pesquisar...',className='input',disabled=false}:{value:string;onChange:(value:string)=>void;options:SearchOption[];placeholder?:string;className?:string;disabled?:boolean}){const id=useId(),selected=useMemo(()=>options.find(o=>o.value===value),[options,value]),[texto,setTexto]=useState(selected?.label||'');useEffect(()=>{setTexto(selected?.label||'')},[selected?.label,value]);function alterar(t:string){setTexto(t);const n=t.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(),exato=options.find(o=>o.label.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase()===n);if(exato)onChange(exato.value);else if(!t)onChange('')}return <><input list={id} className={className} value={texto} disabled={disabled} onChange={e=>alterar(e.target.value)} onBlur={()=>{if(value)setTexto(options.find(o=>o.value===value)?.label||'')}} placeholder={placeholder} autoComplete="off"/><datalist id={id}>{options.map(o=><option key={o.value} value={o.label}/>)}</datalist></>}
