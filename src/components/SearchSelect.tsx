import React,{useEffect,useMemo,useRef,useState}from'react'
export type SearchOption={value:string;label:string}
const normalizar=(v:string)=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLocaleLowerCase('pt-BR')
export default function SearchSelect({value,onChange,options,placeholder='Digite para pesquisar...',className='input',disabled=false}:{value:string;onChange:(value:string)=>void;options:SearchOption[];placeholder?:string;className?:string;disabled?:boolean}){const selected=useMemo(()=>options.find(o=>o.value===value),[options,value]),[texto,setTexto]=useState(selected?.label||''),[aberto,setAberto]=useState(false),caixa=useRef<HTMLDivElement>(null);useEffect(()=>{setTexto(selected?.label||'')},[selected?.label,value]);useEffect(()=>{function fora(e:MouseEvent){if(!caixa.current?.contains(e.target as Node))setAberto(false)}document.addEventListener('mousedown',fora);return()=>document.removeEventListener('mousedown',fora)},[]);const filtradas=useMemo(()=>{const q=normalizar(texto.trim());return options.filter(o=>!q||normalizar(o.label).includes(q)).slice(0,100)},[options,texto]);function alterar(t:string){setTexto(t);setAberto(true);if(value)onChange('')}function escolher(o:SearchOption){onChange(o.value);setTexto(o.label);setAberto(false)}return <div ref={caixa} className="relative w-full"><input className={className} value={texto} disabled={disabled} onChange={e=>alterar(e.target.value)} onFocus={e=>{setAberto(true);e.currentTarget.select()}} onKeyDown={e=>{if(e.key==='Escape')setAberto(false)}} placeholder={placeholder} autoComplete="off"/>{aberto&&!disabled&&<div className="absolute z-[220] left-0 right-0 top-[calc(100%+4px)] max-h-64 overflow-y-auto rounded-xl border border-zinc-200 bg-white shadow-xl">{filtradas.length?filtradas.map(o=><button type="button" key={o.value} onMouseDown={e=>e.preventDefault()} onClick={()=>escolher(o)} className="block w-full px-3 py-2.5 text-left text-sm hover:bg-[#fff0f8] border-b last:border-b-0">{o.label}</button>):<p className="px-3 py-3 text-sm text-zinc-500">Nenhum resultado encontrado.</p>}</div>}</div>}
