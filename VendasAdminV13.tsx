import React,{useEffect,useMemo,useRef,useState}from'react';import{Check,Cloud,Plus,ShoppingBag,Trash2,X}from'lucide-react';import{supabase}from'../supabase';
let filaVenda:Promise<void>=Promise.resolve();type Draft={id:string;cliente_id:string|null;vendedor_id:string|null;data_venda:string;forma_pagamento:string;desconto:number;entrega:number;observacoes:string|null;parcelas:number;primeiro_vencimento:string|null;parcelas_personalizadas:number[];itens:{variante_id:string;quantidade:number;preco_unitario:number}[]};type Cliente={id:string;nome:string};type Vendedor={id:string;nome:string;comissao_percentual:number};type Var={id:string;tamanho:string;estoque:number;ativo:boolean};type Prod={id:string;nome:string;sku:string|null;preco:number;preco_promocional:number|null;promocao:boolean;produto_variantes:Var[]};type Item={variante_id:string;produto:string;tamanho:string;quantidade:number;preco_unitario:number};
export default function VendasAdminV13(){const[clientes,setClientes]=useState<Cliente[]>([]),[vendedores,setVendedores]=useState<Vendedor[]>([]),[produtos,setProdutos]=useState<Prod[]>([]),[itens,setItens]=useState<Item[]>([]),[cliente,setCliente]=useState(''),[vendedor,setVendedor]=useState(''),[forma,setForma]=useState('Pix'),[data,setData]=useState(new Date().toISOString().slice(0,10)),[parcelas,setParcelas]=useState(2),[valoresParcelas,setValoresParcelas]=useState<number[]>([]),[vencimento,setVencimento]=useState(new Date(Date.now()+30*86400000).toISOString().slice(0,10)),[produtoId,setProdutoId]=useState(''),[varianteId,setVarianteId]=useState(''),[qtd,setQtd]=useState(1),[preco,setPreco]=useState(0),[desconto,setDesconto]=useState(0),[entrega,setEntrega]=useState(0),[obs,setObs]=useState(''),[saving,setSaving]=useState(false),[erro,setErro]=useState(''),[ok,setOk]=useState(''),[rascunhoId,setRascunhoId]=useState(''),[nuvem,setNuvem]=useState('Salvo na nuvem');const pronto=useRef(false);async function carregar(){pronto.current=false;await filaVenda;const[c,p,vend]=await Promise.all([supabase.from('clientes').select('id,nome').eq('ativo',true).order('nome'),supabase.from('produtos').select('id,nome,sku,preco,preco_promocional,promocao,produto_variantes(id,tamanho,estoque,ativo)').eq('ativo',true).order('nome'),supabase.from('vendedores_v17_30').select('id,nome,comissao_percentual').eq('ativo',true).order('nome')]);if(c.error||p.error||vend.error)setErro(c.error?.message||p.error?.message||vend.error?.message||'Erro ao carregar.');else{const ps=(p.data||[])as Prod[];setClientes(c.data||[]);setVendedores((vend.data||[])as Vendedor[]);setProdutos(ps);const d=await supabase.rpc('obter_rascunho_venda_v17_30');if(d.error)setErro('Aplique o SQL V17.28 antes de usar: '+d.error.message);else{const r=d.data as Draft;setRascunhoId(r.id);setCliente(r.cliente_id||'');setVendedor(r.vendedor_id||'');setValoresParcelas((r.parcelas_personalizadas||[]).map(Number));setData(r.data_venda||new Date().toISOString().slice(0,10));setForma(r.forma_pagamento||'Pix');setDesconto(Number(r.desconto)||0);setEntrega(Number(r.entrega)||0);setObs(r.observacoes||'');setParcelas(Number(r.parcelas)||2);setVencimento(r.primeiro_vencimento||new Date(Date.now()+30*86400000).toISOString().slice(0,10));setItens((r.itens||[]).map(i=>{const prod=ps.find(x=>x.produto_variantes.some(y=>y.id===i.variante_id)),vari=prod?.produto_variantes.find(y=>y.id===i.variante_id);return{...i,quantidade:Number(i.quantidade),preco_unitario:Number(i.preco_unitario),produto:prod?.nome||'Produto',tamanho:vari?.tamanho||'—'}}));pronto.current=true;setNuvem('Salvo na nuvem')}}}useEffect(()=>{void carregar()},[]);const args=()=>({p_rascunho_id:rascunhoId,p_cliente_id:cliente||null,p_vendedor_id:vendedor||null,p_data_venda:data,p_forma_pagamento:forma,p_desconto:Number(desconto)||0,p_entrega:Number(entrega)||0,p_observacoes:obs,p_parcelas:forma==='Crediário'?parcelas:1,p_primeiro_vencimento:forma==='Crediário'?vencimento:null,p_parcelas_personalizadas:forma==='Crediário'?valoresParcelas:[],p_itens:itens.map(({variante_id,quantidade,preco_unitario})=>({variante_id,quantidade,preco_unitario}))});async function persistir(a=args()){const r=await supabase.rpc('salvar_rascunho_venda_v17_30',a);if(r.error)throw r.error}useEffect(()=>{if(!pronto.current||!rascunhoId)return;setNuvem('Salvando...');const a=args();filaVenda=filaVenda.then(async()=>{try{await persistir(a);setNuvem('Salvo na nuvem')}catch(e){setNuvem('Falha ao salvar');setErro('Falha ao salvar o rascunho: '+(e as Error).message)}})},[rascunhoId,cliente,vendedor,data,forma,desconto,entrega,obs,parcelas,vencimento,valoresParcelas,itens]);const p=useMemo(()=>produtos.find(x=>x.id===produtoId),[produtos,produtoId]),vars=(p?.produto_variantes||[]).filter(v=>v.ativo&&v.estoque>0);function selProduto(id:string){setProdutoId(id);setVarianteId('');const x=produtos.find(p=>p.id===id);setPreco(Number(x?.promocao&&x.preco_promocional!=null?x.preco_promocional:x?.preco)||0)}function adicionar(){const p=produtos.find(x=>x.id===produtoId),v=p?.produto_variantes.find(x=>x.id===varianteId);if(!p||!v){setErro('Selecione produto e tamanho.');return}const ja=itens.find(i=>i.variante_id===v.id)?.quantidade||0;if(qtd<1||qtd+ja>v.estoque){setErro(`Estoque disponível: ${v.estoque}.`);return}setItens(a=>{const i=a.find(x=>x.variante_id===v.id);return i?a.map(x=>x.variante_id===v.id?{...x,quantidade:x.quantidade+qtd,preco_unitario:preco}:x):[...a,{variante_id:v.id,produto:p.nome,tamanho:v.tamanho,quantidade:qtd,preco_unitario:preco}]});setVarianteId('');setQtd(1);setErro('')}const subtotal=itens.reduce((a,i)=>a+i.quantidade*i.preco_unitario,0),total=Math.max(0,subtotal-desconto+entrega);useEffect(()=>{if(forma!=='Crediário')return;const centavos=Math.round(total*100),base=Math.floor(centavos/Math.max(1,parcelas)),resto=centavos-base*Math.max(1,parcelas);setValoresParcelas(atuais=>atuais.length===parcelas&&Math.round(atuais.reduce((a,v)=>a+Number(v||0),0)*100)===centavos?atuais:Array.from({length:Math.max(1,parcelas)},(_,i)=>(base+(i===parcelas-1?resto:0))/100))},[forma,parcelas,total]);async function salvar(e:React.SyntheticEvent){e.preventDefault();setSaving(true);setErro('');setOk('');try{if(!cliente)throw new Error('Selecione o cliente.');if(!vendedor)throw new Error('Selecione o vendedor.');if(!itens.length)throw new Error('Adicione produtos.');if(forma==='Crediário'&&(!vencimento||parcelas<1||parcelas>24))throw new Error('Informe de 1 a 24 parcelas e o primeiro vencimento.');if(forma==='Crediário'&&Math.round(valoresParcelas.reduce((a,v)=>a+Number(v||0),0)*100)!==Math.round(total*100))throw new Error('A soma das parcelas deve ser exatamente igual ao total da venda.');await filaVenda;await persistir();const{data:numero,error}=await supabase.rpc('finalizar_rascunho_venda_v17_30',{p_rascunho_id:rascunhoId});if(error)throw error;setOk(`Venda ${numero} concluída.`);await carregar()}catch(e){setErro(e instanceof Error?e.message:'Erro ao concluir venda.')}finally{setSaving(false)}}async function cancelarRascunho(){if(!confirm('Cancelar esta venda? Todos os dados do rascunho serão descartados.'))return;setSaving(true);try{await filaVenda;const r=await supabase.rpc('cancelar_rascunho_venda_v17_28',{p_rascunho_id:rascunhoId});if(r.error)throw r.error;setOk('Rascunho da venda cancelado.');await carregar()}catch(e){setErro((e as Error).message)}finally{setSaving(false)}}return <form onSubmit={e=>e.preventDefault()} onKeyDown={e=>{if(e.key==='Enter')e.preventDefault()}} className="bg-white border rounded-[22px] p-5 md:p-6 space-y-5"><div className="flex gap-3 items-center"><ShoppingBag className="text-[#c80082]"/><div><p className="text-[10px] uppercase font-black text-[#c80082]">V17.30</p><h2 className="text-xl font-black">Nova venda</h2></div></div><span className={`text-xs flex gap-2 items-center ${nuvem.includes('Falha')?'text-red-600':'text-zinc-500'}`}><Cloud size={14}/>{nuvem}</span>{erro&&<div className="aviso erro">{erro}</div>}{ok&&<div className="aviso ok">{ok}</div>}<div className="grid md:grid-cols-4 gap-3"><Campo label="Cliente *"><select className="input" value={cliente} onChange={e=>setCliente(e.target.value)}><option value="">Selecione</option>{clientes.map(c=><option key={c.id} value={c.id}>{c.nome}</option>)}</select></Campo><Campo label="Vendedor *"><select className="input" value={vendedor} onChange={e=>setVendedor(e.target.value)}><option value="">Selecione</option>{vendedores.map(v=><option key={v.id} value={v.id}>{v.nome} — {Number(v.comissao_percentual).toFixed(2)}%</option>)}</select></Campo><Campo label="Data"><input type="date" className="input" value={data} onChange={e=>setData(e.target.value)}/></Campo><Campo label="Pagamento"><select className="input" value={forma} onChange={e=>setForma(e.target.value)}>{['Dinheiro','Pix','Cartão de Débito','Cartão de Crédito','Crediário'].map(x=><option key={x}>{x}</option>)}</select></Campo></div>{forma==='Crediário'&&<div className="grid sm:grid-cols-2 gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4"><Campo label="Quantidade de parcelas"><input type="number" min="1" max="24" className="input" value={parcelas} onChange={e=>setParcelas(Number(e.target.value))}/></Campo><Campo label="Primeiro vencimento"><input type="date" className="input" value={vencimento} onChange={e=>setVencimento(e.target.value)}/></Campo><div className="sm:col-span-2"><p className="text-xs font-black text-amber-900 mb-2">Defina o valor de cada parcela — soma: R$ {valoresParcelas.reduce((a,v)=>a+Number(v||0),0).toFixed(2)} de R$ {total.toFixed(2)}</p><div className="grid sm:grid-cols-2 md:grid-cols-3 gap-2">{valoresParcelas.map((valor,i)=><Campo key={i} label={`Parcela ${i+1}`}><input type="number" min="0.01" step="0.01" className="input" value={valor} onChange={e=>setValoresParcelas(x=>x.map((v,j)=>j===i?Number(e.target.value):v))}/></Campo>)}</div></div></div>}<div className="rounded-2xl bg-zinc-50 border p-4"><div className="grid md:grid-cols-[2fr_1fr_90px_130px_auto] gap-2"><select className="input" value={produtoId} onChange={e=>selProduto(e.target.value)}><option value="">Produto</option>{produtos.map(p=><option key={p.id} value={p.id}>{p.nome} — {p.sku}</option>)}</select><select className="input" value={varianteId} onChange={e=>setVarianteId(e.target.value)}><option value="">Tamanho</option>{vars.map(v=><option key={v.id} value={v.id}>{v.tamanho} ({v.estoque})</option>)}</select><input type="number" min="1" className="input" value={qtd} onChange={e=>setQtd(Number(e.target.value))}/><input type="number" min=".01" step=".01" className="input" value={preco} onChange={e=>setPreco(Number(e.target.value))}/><button type="button" onClick={adicionar} className="h-11 rounded-xl bg-zinc-950 text-white grid place-items-center"><Plus/></button></div>{itens.map(i=><div key={i.variante_id} className="mt-2 p-3 bg-white border rounded-xl flex justify-between text-sm"><span><b>{i.produto}</b> • {i.tamanho} • {i.quantidade} un.</span><span className="flex gap-3"><b>R$ {(i.quantidade*i.preco_unitario).toFixed(2)}</b><button type="button" onClick={()=>setItens(a=>a.filter(x=>x.variante_id!==i.variante_id))} className="text-red-600"><Trash2 size={14}/></button></span></div>)}</div><div className="grid md:grid-cols-3 gap-3"><Campo label="Desconto"><input type="number" min="0" step=".01" className="input" value={desconto} onChange={e=>setDesconto(Number(e.target.value))}/></Campo><Campo label="Entrega"><input type="number" min="0" step=".01" className="input" value={entrega} onChange={e=>setEntrega(Number(e.target.value))}/></Campo><Campo label="Observações"><input className="input" value={obs} onChange={e=>setObs(e.target.value)}/></Campo></div><div className="flex justify-between items-end"><div><p>Subtotal: <b>R$ {subtotal.toFixed(2)}</b></p><p className="text-xl">Total: <b>R$ {total.toFixed(2)}</b></p></div><div className="flex gap-2"><button type="button" disabled={saving} onClick={cancelarRascunho} className="h-12 px-5 rounded-xl border text-red-700 font-black inline-flex gap-2 items-center"><X size={16}/>Cancelar venda</button><button type="button" onClick={salvar} disabled={saving||nuvem.includes('Falha')} className="h-12 px-6 rounded-xl bg-[#c80082] text-white font-black inline-flex gap-2 items-center"><Check size={16}/>{saving?'Concluindo...':'Concluir venda'}</button></div></div></form>}
function Campo({label,children}:{label:string;children:React.ReactNode}){return <label><span className="text-[11px] font-bold text-zinc-600 block mb-1.5">{label}</span>{children}</label>}
